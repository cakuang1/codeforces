  #include <bits/stdc++.h>
  using namespace std;

  #ifdef LOCAL
  #include "algo/debug.h"
  #else
  #define debug(...) 42
  #endif

  using ll = long long;
  using ld = long double;
  const ll mod = 1e9 + 7;
  const ll N = 2e5 + 10;
  const ll inf = 1e9;
  const ll linf = 1e18;



  int main() {
      ios::sync_with_stdio(false);
      cin.tie(NULL);
      ll t; 
      cin >> t; 
    
      while (t --) {
        // how to see thsi 

        int n;
        cin >> n;

        if (n == 1) {
            cout << -1 << endl; 
            continue;
        }
        for (int i = 0 ; i < n - 1; i ++ ) {
            cout << 5;
        }    
        cout << 8 << endl;
      }
        // somebody here stinks 

      // duplciated w  

      return 0;

            
  }