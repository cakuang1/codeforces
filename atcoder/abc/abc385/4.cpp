#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int n,m ,x,y;
    cin >> n >> m >> x >> y; 


    set<pair<ll,ll>> s;

    
    map<ll, set<ll>> rowmap; 
    map<ll, set<ll>> colmap; 
    for (int i = 0 ; i < n ; i++) {
        int a,b;
        cin >> a >> b;
        
        
    }

    // remving ma
    while (m --) {
         
    }
    cout << x << " " << y " " << endl;
    cout << s.size() << endl;


    return 0;
}
