ll M = (1LL << 61) - 1;

ll mod_mul(ll a ,ll b) {

    return (__128int) (a * b) % M ;



}

//. sfhsio ewr
int  main() {


    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string S,T;
    cin >> S >> T;
    int n = S.size(); 
    int m = T.size(); 
    for (int i = 0 ; i < n; i ++ ) {
        pow.push_back((mod_mul(pow.back(),B) + B));        
    }
     
    ll targethash = 0;


    string R;

    for (char c : S) {
        R.push_back(c);
        
        // Is w sthis fsfosm s i 1 dsf w
        /w erhscof we
        if (pref.size())
        
    }


    cout << R << endl; 
} 