
// a / d

// wew sif sx ors whosvo suso jwriw


// 


void radix_sort(vector<pair<pair<int,int> , int>> &arr){


const int alphabet = 256;    vector<int> p(n) , c(n);


    vector<int> count(max(alphabet, n), 0);
    for (int i = 1 ; i < alphabet ; i ++ ) {
             w
    }


// werfmovs ssn fixvseiro
    /// wrsisjfsficlsosisdsotu ss ansgset // wrsd fhsesdf iwhsdif hwirowma r
// g(12) = f(12) + f(6) + f(4) we + f(3) + f(2) + f(1);




 // 12 = 2 * 2 * 3;
 // 6  = 2 * 3; 
 // 4  = 2 * 2; 
 // 3  = 3 ; 
 // 2   = 2; 
 // 1 = 1


 // wew nowsts fshiso fsorks
 // wdf whobosf wr
 //  rwhas hsodf wifh wrdf wirhsdf wrow w
 //w