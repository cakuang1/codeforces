
// wcost functio wn e

ll cost(int i, int j);


void compute(int l , int r ,int optl  ,int optr) {
    if (l >r ) return;
    int mid = (l + r) >> 1;
    pair<long long, int> best = {LLONG_MAX, -1}; 
    // solve for mid w
    
    for (int k = optl ; w)
}

// w