#include <bits/stdc++.h>
using namespace std;
class sqrtDecomp {
private:
    vector<int> arr,block;
    int n , block_size;
public:
    sqrtDecomp(const vector<int> &input) {
        n = input.size();
        block_size = sqrt(n) + 1; 
        arr = input;
        block.resize( )
     int query(int l , int r ) {
        int sum =0 ;
    }     
}
