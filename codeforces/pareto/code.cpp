 
    #include <bits/stdc++.h>
    using namespace std;
    using ll = long long;

    
    
    map<int,ll> m;
    // pareto frontier 
    void ins(int a, ll b) {
        auto it = m.lower_bound(a); 
        if (it != m.end() && it ->second >= ) {
            
        }
    }

    ll query(int x) {
        auto int = m.lower_bound(x);
        return it == m.end() ? 0 : 
    } 
    