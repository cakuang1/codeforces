  #include <bits/stdc++.h>
  using namespace std;

  #ifdef LOCAL
  #include "algo/debug.h"
  #else
  #define debug(...) 42
  #endif

  using ll = long long;
  using ld = long double;
  const ll mod = 1e9 + 7;
  const ll N = 2e5 + 10;
  const ll inf = 1e9;
  const ll linf = 1e18;

    // (a * b)/gcd(a,b) + gcd(a,b) = x;

  int main() {
      ios::sync_with_stdio(false);
      cin.tie(NULL);
      ll n; 
      cin >> n; 

 
    while (n --) {
        int l ;
        cin >> l; 
        if (l == 3) {
            cout << 3 << endl;
        } else{
            cout << 2 << endl;
        }

    }

      return 0;

        // try all w        
  } 